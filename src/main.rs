use::std::io::{
    stdin,
    prelude::*
}
use::std::env;
use::hyper::{header, Body, Client, Request};
use::hyper_tls::HttpsConnector;
use::serde_deserve::{Serialize, Deserialize};
use::spinners::{Spinner, Spinners}



fn main() {
    println!("Hello, world!");
}
